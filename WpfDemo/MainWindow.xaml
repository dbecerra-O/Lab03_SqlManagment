<Window x:Class="WpfDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="350">
    <Grid>
        <TextBox x:Name="txtSearch" HorizontalAlignment="Left" VerticalAlignment="Top" Width="200" Margin="10,10,0,0" />
        <Button Content="Buscar" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="220,10,0,0" Click="Button_Click"/>
        <DataGrid x:Name="dataGridProducts" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,40,10,10" Height="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product ID" Binding="{Binding ProductId}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
